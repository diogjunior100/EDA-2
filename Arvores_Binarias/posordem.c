#include <stdio.h>

typedef struct no
{
    int dado;
    struct no *esq, *dir;
} no;



void pos_ordem (no *raiz){


    if (raiz == NULL)
    {
        return raiz;
    }
    pos_ordem()
    
    
    pos_ordem();
}
